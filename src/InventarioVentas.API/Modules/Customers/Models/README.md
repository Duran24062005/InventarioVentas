# Models de Clientes

Aqui viven la entidad `Cliente` y los modelos propios de la informacion del comprador.

La entidad debe representar el estado que se persiste y sus relaciones con ventas. Los contratos HTTP se mantienen en `DTOs` y el mapeo de base de datos en `Data/Configurations`.
